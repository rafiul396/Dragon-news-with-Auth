import React, { createContext } from 'react';
const AuthContext = createContext();


const AuthProvider = () => {
    return (
        <AuthContext>

        </AuthContext>
    );
};

export default AuthProvider;